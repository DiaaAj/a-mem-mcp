# Quick Setup Template for MCP Server
# Copy this to .env in the project root and configure

# Basic OpenAI Configuration (recommended)
LLM_BACKEND=openai
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=your-key-here
EMBEDDING_MODEL=all-MiniLM-L6-v2
EVO_THRESHOLD=100
